export class Member {
  public name: string;
  public intro: string;
  public description: string;

  constructor(name: string, intro: string, description: string) {
    this.name = name;
    this.intro = intro;
    this.description = description;
  }
}
